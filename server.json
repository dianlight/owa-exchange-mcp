{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.nhype/owa-exchange-mcp",
  "title": "OWA Exchange MCP Server",
  "description": "MCP server for Microsoft Exchange / OWA — email, calendar, directory, availability",
  "repository": {
    "url": "https://github.com/nhype/owa-exchange-mcp",
    "source": "github"
  },
  "version": "2.0.0-beta.0",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "exchange-mcp-server",
      "version": "2.0.0-beta.0",
      "runtimeHint": "uvx",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "EXCHANGE_OWA_URL",
          "description": "Base URL of your OWA instance (e.g. https://owa.example.com)",
          "required": true
        },
        {
          "name": "EXCHANGE_BROWSER_PROFILE_DIR",
          "description": "Path to the persistent browser profile directory (default: .browser-profile)",
          "required": false
        },
        {
          "name": "EXCHANGE_MASTER_PASSWORD",
          "description": "Master password to auto-login at startup using stored encrypted credentials",
          "required": false
        },
        {
          "name": "EXCHANGE_HEADLESS",
          "description": "Set to false/0 to run the browser with a visible window (for debugging)",
          "required": false
        }
      ]
    }
  ]
}
